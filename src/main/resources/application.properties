spring.application.name=plain-project

# Datenbank: lokal startet Spring Boot PostgreSQL aus compose.yaml.
# In anderen Umgebungen über Umgebungsvariablen setzen:
# SPRING_DATASOURCE_URL, SPRING_DATASOURCE_USERNAME, SPRING_DATASOURCE_PASSWORD

management.endpoints.web.exposure.include=health
